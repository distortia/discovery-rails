We will use [semantic versioning](http://semver.org/)

Branching works like this:

1) pull down repo
2) checkout develop
3) create your branch using this format and release#(x-x-x-decription-of-feature): Ex: `0.2.0-login-page`
4) develop your branch
5) checkout develop
6) merge your branch: `merge [branchname]`
7) fix conflicts
8) pull develop from server
9) more merges
10) commit branch
11) ???
12) profit

Nick will merge into master when we deem a release acceptable